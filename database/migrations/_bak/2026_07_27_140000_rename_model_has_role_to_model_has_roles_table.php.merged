<?php

declare(strict_types=1);

use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Modules\User\Models\User;
use Modules\Xot\Database\Migrations\XotBaseMigration;

/*
 * Rinomina la tabella model_has_role in model_has_roles per conformità
 * con la convenzione pluralizzata di Laravel Permission.
 *
 * La tabella esiste come model_has_role (singolare) ma Laravel Permission
 * si aspetta model_has_roles (plurale) per le query.
 */
return new class() extends XotBaseMigration
{
    protected ?string $model_class = User::class;

    public function up(): void
    {
        // Rinomina la tabella se esiste model_has_role
        if (Schema::hasTable('model_has_role') && ! Schema::hasTable('model_has_roles')) {
            Schema::rename('model_has_role', 'model_has_roles');
        }
    }

    public function down(): void
    {
        // Rinomina indietro
        if (Schema::hasTable('model_has_roles') && ! Schema::hasTable('model_has_role')) {
            Schema::rename('model_has_roles', 'model_has_role');
        }
    }
};